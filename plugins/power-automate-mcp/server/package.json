{
  "name": "power-automate-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Free, local MCP server for Microsoft Power Automate cloud flows. Reuses your Azure CLI login — no paid third-party service. Drop-in replacement for the FlowStudio MCP tool surface (live tools).",
  "type": "module",
  "bin": {
    "power-automate-mcp": "build/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node build/index.js",
    "driver": "node driver.mjs",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
